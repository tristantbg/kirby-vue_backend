title: Home
preset: page
pages: false
files: false
fields:
  blocks:
    label: Intro
    type: builder
    columns: 1
    fieldsets:
      images:
        label: Images
        fields:
          files:
            type: files
            query: kirby.collection('index').files.template('image')
            uploads: image
            layout: cards
            size: tiny
